package com.example.btlapp;

import java.util.ArrayList;
import java.util.List;

public class TrafficSignRepository {
    public static List<TrafficSign> getAllSigns() {
        // This will be empty or unused now as we load from JSON, 
        // but kept to avoid breaking existing code if any.
        return new ArrayList<>();
    }
}
